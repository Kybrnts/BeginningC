/* Exercise 9.4 */
/*
 * Implement a function that has the followning prototype
 * char *to_string(int count, double first, ...);
 * This function should return a string that contains string representations of the second and subsequent arguments,
 * each to two decimal places and separated by commas. The first argument is a count of the number of arguments that
 * follow. Write a suitable version of main() to demonstrate the operation of your function.
 */
#include <stdio.h>
#include <stdarg.h>

char *to_string(int count, double first, ...);

int main(int argc, char *argv[]) {

  return 0;
}

